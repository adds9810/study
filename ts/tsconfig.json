{
  "compilerOptions": {
    "strict": true, //모든 엄격한 타입 검사 옵션을 활성화
    "module": "commonjs",
    "moduleResolution": "node",
    "lib": ["es2015", "es2016", "es2017", "es2018", "es2019", "es2020"], // 사용할 js 버전 범위
    "target": "ES5", //트랜스파일 할 대상 자바스크립트 버전 설정, 대부분 es5
    "outDir": "./dist", //baseUrl 설정값을 기준으로 했을 때 하위 디렉터리의 이름
    "esModuleInterop": true //true로 설정 해놓으면 export default가 없는 라이브러리도 * as 를 사용하지 않고 불러올 수 있음
  },
  "exclude": ["node_modules"],
  "include": ["src/**/*"]
}
